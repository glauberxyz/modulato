# create-modulato

Scaffold a Modulato site:

```sh
npm create modulato@latest my-site
```

Non-interactive by design — one argument, no prompts, no side effects beyond
writing the directory. The scaffolded project ships MODULATO.md (the full
framework reference) and CLAUDE.md (agent guide).
