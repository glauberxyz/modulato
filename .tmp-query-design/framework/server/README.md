# @modulato/server

Modulato's server rendering: full-HTML React SSR, first-load intro hiding,
and the server-action runner shared by the dev middleware and production
functions.

Part of [modulato](https://www.npmjs.com/package/modulato) — scaffold a site
with `npm create modulato@latest`.
