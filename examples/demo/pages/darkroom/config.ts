export function meta() {
  return {
    title: 'Darkroom — Halftone',
    description:
      'Every uniform in the halftone shader, live: ruling, contrast, softness, grid noise, dot shape and the four screen angles.',
  }
}

// The panel scrolls, the scene does not.
export const scroll = false
